services:
  - type: web
    name: cad-nest-pro
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node src/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
